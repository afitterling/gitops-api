dbms: postgres
orm: sqlalchemy
output: flask
infrastructure: heroku

model:
  name: user
  attributes:
    id:
      type: number
      key: primary
      #index: true
      unique: true

model:
    name: item
    table_name: items
    relates: user
    relation: onetomany
    attributes:
        id:
            type: number
            key: primary
            unique: true
        name:
            type: string
            constrains:
              unique: false

api: rest
security: 
  name: jwt
  tokens:
    - refresh
    - access_token

resource:
  name: UserList
  types:
    #- GET
    #  model: User
    #    - findBy: id
    #  jwt: required
    - POST
      model: User
      jwt: required 

endpoint:
  name: user
  url: /user
    #- GET: /user/<id:int>
    - POST: /users








    